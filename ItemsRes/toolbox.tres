[gd_resource type="Resource" load_steps=2 format=2]

[ext_resource path="res://ItemPawnGD.gd" type="Script" id=1]

[resource]
script = ExtResource( 1 )
name = "Toolbox"
spriteFrame = 4
sizeFloor = Vector2( 20, 8 )
sizeGrid = Vector2( 3, 2 )
sizeSprite = Vector2( 33, 22 )
type = 9
intArray = [  ]
guiFrame = 0
textField = ""
useCommands = "~ defining region for pickup action
define pickup
message I think this might be useful to combine things

~ defining region for use action
define use
inventory toolbox toggle

~ defining region for drop action
define drop
inventory toolbox disable"
